# متجر THREAD - thread-ecommerce 🛍️

متجر إلكتروني عصري متكامل مبني باستخدام Next.js و Firebase، مصمم بتجربة مستخدم عربية (RTL) احترافية.

## ✨ المميزات الرئيسية
- **واجهة مستخدم متطورة**: تصميم سريع الاستجابة (Responsive) يدعم الهواتف والأجهزة اللوحية.
- **الوضع الليلي الذكي**: دعم كامل للـ Dark Mode مع تصحيح كافة الألوان.
- **إتمام الشراء عبر WhatsApp**: نظام سلة مشتريات يوجه الطلبات مباشرة إلى واتساب التاجر.
- **لوحة تحكم للمنتجات**: إمكانية إضافة وتعديل وحذف المنتجات ورفع صورها.
- **دليل مقاسات وسياسات**: صفحات تفاعلية لدليل المقاسات وسياسة الإرجاع.

## 🚀 كيفية الرفع على GitHub
1. قم بإنشاء مستودع (Repository) جديد على GitHub باسم `thread-ecommerce`.
2. افتح الـ Terminal في مجلد المشروع ونفذ الأوامر التالية:

```bash
git init
git add .
git commit -m "النسخة الأولى من متجر THREAD"
git branch -M main
git remote add origin https://github.com/[اسم_حسابك]/thread-ecommerce.git
git push -u origin main
```

## 🌐 النشر على Vercel
يمكنك النشر بسهولة عبر اتباع الخطوات التالية في الـ Terminal الخاص بك:

1. **تسجيل الدخول**:
   ```bash
   npx vercel login
   ```
2. **بدء النشر**:
   ```bash
   npx vercel
   ```
3. **النشر للإنتاج (Production)**:
   ```bash
   npx vercel --prod
   ```

**ملاحظة هامة**: تأكد من إضافة متغيرات البيئة (Environment Variables) في إعدادات مشروعك على Vercel لضمان عمل Firebase:
- `NEXT_PUBLIC_FIREBASE_API_KEY`
- `NEXT_PUBLIC_FIREBASE_PROJECT_ID`
- `NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN`
- `NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET`

## 🛠️ التقنيات المستخدمة
- **Next.js 15** (App Router)
- **Firebase** (Firestore & Auth & Storage)
- **Tailwind CSS** (Styling)
- **ShadCN UI** (Components)
- **Lucide React** (Icons)

---
تم التطوير بواسطة **Firebase Studio AI**